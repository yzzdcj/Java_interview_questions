###缓存过期策略
####解决方案：
    1.惰性删除:用到某key时再删除
        优点：cpu友好
        缺点：内存不友好
    2.定期删除：每隔一段时间对过期key处理
        实现：Slow模式：默认频率10hz，处理不少于25ms
             Fast模式：默认间隔不少于2ms，处理不少于1ms
        优点：内存消耗较少
        缺点：难以确定频率
###缓存淘汰策略：内存满了，选择哪些key进行删除
####解决方案：
    1.默认是noeviction，满了就报错
    2.常用的有lru,lfu