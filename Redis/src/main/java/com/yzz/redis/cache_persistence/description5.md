###缓存持久化
####描述：
    指将数据保存到磁盘、数据库等非易失性存储，重启后不丢失
####解决方案：
    1.RDB：定期将内存中的数据存储到磁盘中的rdb文件当中，redis宕机重启后会从rdb文件中恢复数据
        优点：rdb文件小，恢复速度快
        缺点：数据完整性较差，占用较多cpu资源
    2.aof；把对Redis的操作命令存储到一个文件中，重启后从文件中执行命令恢复数据
        优点：完整性较高，资源占用较低
        缺点：恢复速度慢，文件体积大
